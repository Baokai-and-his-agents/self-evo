# Run Summary · run-002

- run_id: 2026-06-18-run-002
- worker: local-code-worker-01
- issue: 5 — [Build] Implement run validator and Claude Code safety hooks
- issue_url: https://github.com/Baokai-and-his-agents/self-evo/issues/5
- branch: agent/local-code-worker-01/5-hooks-validator
- started_at: 2026-06-18T17:11:00+0800
- lease_until: 2026-06-18T20:11:00+0800
- github_account: clawbie (agent account)

> **Iteration 2 — review response (jlcbk `CHANGES_REQUESTED`, 2026-06-18).**
> This update addresses every actionable item in the latest review on Draft PR #6.
> See the "Review response" section below for the item-by-item mapping and the
> exact verification results. The committed rollout default remains `audit`.
>
> **Iteration 3 — follow-up correctness fixes (jlcbk independent re-test, 2026-06-18).**
> Two remaining issues fixed: (a) run-summary matching now requires the **exact
> run identity** (a bare `Issue #5` reference no longer satisfies a different run
> of the same issue); (b) PreToolUse now also catches PowerShell `-LiteralPath`
> (Set/Add/Clear-Content, Out-File, Tee-Object, New-Item, Copy/Move-Item
> destination), Bash `dd of=`, and `install SRC DEST` — with no false positives on
> `pip`/`npm`/`make install`. Counts: offline matrix **78/78**, integration
> **27/27**, validator **9/9 PASS**. Variable-indirect and arbitrary here-doc /
> interpreter writes remain documented as Stop-backstop-only cases; the real-git
> committed-diff backstop is preserved.

## Goal

Implement MVP 1.5: a deterministic read-only run validator plus Claude Code
PreToolUse/Stop safety hooks that turn the repository rules into executable
guardrails. Rollout must be incremental; the committed default mode stays
`audit` until the human owner approves enforcement.

## Files created

Implementation (all single-source-of-truth Python 3, stdlib only):
- `scripts/policy.json` — shared guardrail contract (paths, branches, secret
  patterns, dangerous-command patterns, rollout-mode semantics, default `audit`).
- `scripts/_policy.py` — pure classification logic (paths, commands, tool-use,
  decision synthesis, mode resolution). No I/O in the pure helpers.
- `scripts/validate_run.py` — read-only run validator (PASS/WARN/BLOCK).
- `scripts/validate-run.ps1` — Windows PowerShell entrypoint (wraps the python
  validator, preserves exit code).
- `scripts/hooks/pretooluse.py` + `scripts/hooks/pretool-use.ps1` — PreToolUse hook.
- `scripts/hooks/stop.py` + `scripts/hooks/stop.ps1` — Stop hook (anti-loop capped).
- `scripts/hooks/config.json` — committed mode = `audit`.
- `scripts/hooks/claude-settings.sample.json` — sample project hook wiring.
- `scripts/hooks/__init__.py`, `scripts/tests/__init__.py` — package markers.
- `scripts/tests/test_matrix.py` — offline deterministic test matrix.
- `scripts/README.md` — install / mode enabling / rollback / tests / JSON I/O
  contract / limitations / false positives.
- `scripts/.gitignore`, `data/.gitignore` — ignore `__pycache__` and `data/audit/`.

Coordination / governance:
- `state/claims/5.json` — claim for issue #5.
- `state/heartbeat.json` — worker registered (later set stopped).
- `data/tasks/TASKS.md` — issue #5 mirror.
- `data/proposals/rule_changes/2026-06-18-issue5-claude-hooks-wiring.md` —
  proposal for `jlcbk` to wire hooks into protected `.claude/settings.json`.
- `data/tasks/REVIEW.md` — review items.
- `data/runs/2026-06-18/run-002.summary.md` — this file.

## Files NOT modified (protected)

- No `rules/**` edits.
- No `.github/**` edits.
- No `.claude/**` edits (project Claude config is protected; wiring proposed
  under `data/proposals/rule_changes/`).
- No `main` push, no force push, no merge of my own PR.

## Checks and tests (exact results — iteration 2)

1. `python -m py_compile` over all six `.py` files (`_policy`, `validate_run`,
   `hooks/pretooluse`, `hooks/stop`, `tests/test_matrix`, `tests/test_integration`)
   → **compile OK**.
2. Offline test matrix `python scripts/tests/test_matrix.py` →
   **78 passed, 0 failed, exit 0**. Covers the original Issue #5 matrix plus
   the review regressions: PowerShell + Bash command/shell-write detection
   (Set-Content/Out-File/`>`/`>>`/`tee`/`cp`/interpreter `-c`, and iteration-3
   PowerShell `-LiteralPath` / Bash `dd of=` / `install SRC DEST` with
   `pip`/`npm`/`make install` false-positive guards), audit-summary secrecy,
   draft-PR `isDraft` gate, run-identity specificity (run-001 ≠ run-002), issue
   derivation, the Stop emit contract (exit 0 + `{decision:"block"}`), the
   PreToolUse emit contract (exit 0 + `permissionDecision:"deny"`), and the
   anti-loop cap (`[(0,block),(0,block),(0,None)]`).
3. Real-git / real-subprocess integration tests
   `python scripts/tests/test_integration.py` → **27 passed, 0 failed, exit 0**.
   These create throwaway git repos under the system temp dir and run real
   `git`/`python` subprocesses (not monkeypatched accessors): they prove a clean
   committed branch is no longer "zero changes", that staged/unstaged/untracked/
   renamed/deleted files are all unioned in, that the Stop committed-diff backstop
   catches a `rules/**` write that bypassed PreToolUse, that the validator runs as
   a real subprocess on this Windows/GBK machine without crashing, that UTF-8 git
   output decodes safely, that the on-disk audit log contains no raw
   commands/secrets, and (iteration 3) that run-summary matching with real files
   rejects an unrelated run-001 summary while accepting the matching run-002 file.
4. Read-only validator `python scripts/validate_run.py --issue 5 --date
   2026-06-18` at run end → **9 PASS / 0 WARN / 0 BLOCK, exit 0**. (Previously
   this crashed with `UnicodeDecodeError`/`AttributeError` on this machine; and
   `changed_files_have_draft_pr` previously reported "no tracked file changes"
   because committed branch changes were invisible — both fixed.)
5. Windows PowerShell 5.1 wrapper entrypoints exercised via `powershell.exe`:
   - `validate-run.ps1 -Issue 5 -AsJson` → exit 0, 9/9 PASS JSON ✓
   - `pretool-use.ps1` (main push, full-enforce) → exit 0, deny JSON with
     `permissionDecisionReason` surfaced ✓
   - `stop.ps1` (full-enforce) → exit 0, allow `{}` on this complete repo state ✓
   - `stop.ps1` with `SELF_EVO_STOP_GUARD=1` → exit 0, allow ✓

No destructive command was executed during any test. Dangerous operations were
simulated by feeding command **strings** into the pure classifier, by injecting
fake state, and by committing to throwaway temp git repos (never this repo).

## Review response (jlcbk CHANGES_REQUESTED, 2026-06-18)

Item-by-item mapping of the review's actionable items:

1. **Validator now sees committed branch changes.** `changed_files()` is the
   union of the committed diff vs a robustly-resolved base ref
   (`SELF_EVO_BASE_REF` > `origin/main` > `main` > …) plus staged/unstaged/
   untracked/renamed/deleted working-tree files (parsed NUL-safe with
   `-z`/`core.quotepath=false`). A clean committed branch no longer reports zero
   changes (verified: 22 files here, was 0). Added real-git integration tests.
2. **PowerShell + shell-write coverage.** `classify_tool_use` now treats the
   first-class `PowerShell` tool identically to `Bash`, and a new
   `classify_shell_write` catches redirections, `tee`/`Tee-Object`, the
   PowerShell content cmdlets, `cp`/`mv`/`Copy-Item`/`Move-Item` destinations,
   `[System.IO.File]::WriteAll*`, and the common `python|node|perl -c
   "...open(...)..."` inline pattern. Because arbitrary interpreter writes cannot
   be perfectly pre-classified, the Stop hook's committed-diff check is the
   reliable backstop (integration-tested). Bypass regression tests added;
   limitations documented honestly.
3. **Windows subprocess decoding fixed.** `_run()` decodes explicitly as UTF-8
   with `errors="replace"` and tolerates `None` output; the real validator now
   runs on this Windows/Chinese-locale machine (was `UnicodeDecodeError` →
   `AttributeError`). Subprocess integration test added.
4. **Directly-installable Windows hook config.** `claude-settings.sample.json`'s
   `hooks` block is exec-form `python` + `${CLAUDE_PROJECT_DIR}` (no `python3`,
   no `pwsh`, no fake `_windows_command` inside hook objects); valid against the
   current official schema. Non-Windows and `.ps1`-via-`powershell.exe`
   alternatives kept in separate top-level keys.
5. **Audit log never persists raw commands/secrets.** Only minimal metadata is
   logged (codes, a path *zone*, command *length* + *sha256 prefix*). Tests
   prove tokens, `Authorization`/`Bearer` values, passwords, and command payloads
   do not appear in the on-disk log.
6. **`has_draft_pr_for_branch()` now requires `isDraft == true`** — a ready/open
   non-draft PR no longer satisfies the draft gate.
7. **Run-summary is run-specific.** Identity is derived from the active claim's
   `run_id` (then heartbeat); an unrelated run-001 summary on the same date no
   longer satisfies run-002. Missing/ambiguous identity is reported explicitly.
8. **Stop hook no longer hardcodes Issue #5.** The issue is derived from
   `SELF_EVO_ISSUE` > the agent branch name > the active claim/heartbeat; missing
   context is surfaced as a `systemMessage` without trapping the worker.
9. **Hook output/exit-code contract re-checked** against current official docs:
   PreToolUse deny is exit 0 + `hookSpecificOutput.permissionDecision:"deny"`
   (exit 2 would drop the reason); Stop block is exit 0 + top-level
   `{"decision":"block","reason":...}`; advisories use `systemMessage`. Denials
   and Stop blocks now surface reasons on Windows.

## Current rollout mode

**`audit`** — the committed default in `scripts/hooks/config.json` and
`scripts/policy.json#default_rollout_mode`. No tool action is blocked until
`jlcbk` approves a higher mode.

## Decisions

- **Single Python implementation + PowerShell wrappers** rather than two parallel
  codebases. The Windows `.ps1` entrypoints resolve `python`/`py -3` and forward
  stdin/args; the three surfaces (hooks, validator, tests) share `_policy.py`
  and `policy.json`, so they cannot diverge. Python 3 is a documented
  prerequisite (the agent does not install global software; no `pip` deps —
  stdlib only, since `pyyaml` is unavailable and must not be globally installed).
- **Mode config** lives in `scripts/hooks/config.json` (protected hook
  implementation), overridable by env `SELF_EVO_ROLLOUT_MODE`. This keeps the
  committed default auditable and prevents a confused worker from silently
  flipping enforcement.
- **`.github/**` and `docs/**` are WARN (proposal_required), not BLOCK**, so
  template edits are visible without trapping the worker.
- **`scripts/**` is protected hook implementation**: in enforce modes a worker
  cannot edit hooks/config via editor tools. Development happens in audit mode
  on an agent branch (the hook inspects tool calls, not commits).

## Known limitations (iteration 2, stated honestly)

- **Command classification is substring-based.** Obfuscated/aliased dangerous
  commands may evade PreToolUse detection; benign strings containing the
  substrings may false-positive.
- **Shell-write detection is best-effort, not a shell parser.** Common
  redirections, write cmdlets, `tee`/`cp`/`mv`, `.NET` file writes, and the
  common inline `open(...)` interpreter pattern are caught, but **arbitrary
  interpreter writes cannot be perfectly classified before execution.** The
  Stop hook's committed-diff check is the reliable backstop for anything that
  lands on the branch.
- **Secret detection is path/name heuristic.** Enforce modes can deny reads of
  sensibly-named non-secret files; the audit log records only metadata so a
  misclassification never persists a payload.
- **`scripts/**` is protected hook implementation** in enforce modes; hook
  development happens in `audit` mode on an agent branch via `git`.
- **Stop calls `gh` for labels at stop time** (read-only, 15s timeout each) —
  within Claude Code's hook timeout but adds latency in modes that run those
  checks.
- **`.ps1` wrappers require `python` on PATH** (documented prerequisite; not
  installed by the agent). The directly-installable config bypasses the wrapper
  entirely via exec-form `python`.
- The validator is deterministic *given live repo state*, not state-independent.

## False-positive risks

- `read_likely_secret` on files whose names contain TOKEN/KEY/SECRET/credential.
- `protected_branch_push` matching `push origin main` inside a larger command
  (e.g. an `echo`).
- Force-push detection on `push +ref` style invocations is conservative.
See `scripts/README.md` for the full list.

## Governance proposals requiring `jlcbk`

- `data/proposals/rule_changes/2026-06-18-issue5-claude-hooks-wiring.md` —
  create `.claude/settings.json` from the sample, confirm Python 3 on the
  workspace, and decide when (if ever) to raise the mode above `audit`.

## Failures / blockers

None blocking. Two real bugs were found and fixed by the offline tests before
delivery: (a) `_norm` stripped leading dots from dotfiles (`.env`→`env`,
`.github`→`github`), fixed to strip only a literal `./` prefix; (b) the
porcelain parser lost the first char of the first file because `_git` used
`.strip()` (eating the significant leading space of `' M path'`), fixed to
`.rstrip()`. Both were caught by the test matrix — evidence the guardrails work.

## Delivery

- Draft PR opened by `clawbie`, linked to issue #5 (URL in the final issue comment).
- Claim released, heartbeat stopped, lease removed, TASKS.md synchronized, issue
  moved to `status:review` once the PR exists.

## Resource usage

- `github-repo-issue-and-branch-work` (comment / label / agent branch / draft PR):
  within approved scope, cost 0.
- No network, no paid API, no secrets accessed.

## Next steps

- `jlcbk`: review and merge the draft PR; optionally install the hooks per the
  governance proposal; decide on raising the rollout mode.
