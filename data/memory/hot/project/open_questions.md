# Open Questions

- Should local hooks be added to detect accidental `rules/**` edits?
- Should sandbox execution use Docker or local isolated directories first?
- When should SQLite FTS or embeddings be introduced?
- Should GitHub Projects be added after issue labels are enough for MVP?
- What metadata should control hot-to-cold memory transitions?
